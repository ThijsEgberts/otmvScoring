<?xml version="1.0" encoding="utf-8"?>
<shape  xmlns:android="http://schemas.android.com/apk/res/android"
    android:name="box_hollow_orange"
    android:shape="rectangle"
    android:scaleType="fitXY">
    <solid android:color="@color/transparent"/> <!-- Set the background color -->
    <corners android:radius="@dimen/button_radius"/> <!-- Set the radius of the corners -->
    <stroke android:color="@color/orange_base"
        android:width="3dp" />
</shape>